<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent" android:layout_height="match_parent"
                xmlns:app="http://schemas.android.com/apk/res-auto">
    <android.support.design.widget.AppBarLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/appbarLayoutProfileActivity"
            android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar">

        <include
                android:id="@+id/toolbarTop"
                layout="@layout/toolbar_top"/>

        <android.support.design.widget.TabLayout
                android:id="@+id/tabsProfileActivity"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:tabMode="fixed"
                app:tabIndicatorColor="@color/md_orange_100"
                app:tabIndicatorHeight="4dp"
                app:tabGravity="fill"/>
    </android.support.design.widget.AppBarLayout>

    <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/bottomMenuContainerMainActivity"
            android:layout_alignParentBottom="true"
            android:orientation="vertical">
        <include
                android:id="@+id/toolbarBottomMainActivity"
                layout="@layout/toolbar_bottom"/>
    </LinearLayout>
    <android.support.v4.view.ViewPager
            android:id="@+id/viewpagerProfileActivity"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layout_behavior="@string/appbar_scrolling_view_behavior"
            android:layout_below="@id/appbarLayoutProfileActivity"
            android:layout_above="@+id/bottomMenuContainerMainActivity"/>
</RelativeLayout>
